display:
  default:
    display_options:
      exposed_form:
        options:
          submit_button: Apply
          exposed_sorts_label: 'Sort by'
      arguments:
        uid:
          exception:
            title: All
