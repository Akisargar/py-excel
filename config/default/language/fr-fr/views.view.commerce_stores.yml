display:
  default:
    display_options:
      pager:
        options:
          expose:
            items_per_page_options_all_label: '- All -'
      exposed_form:
        options:
          submit_button: Apply
          exposed_sorts_label: 'Trier par'
